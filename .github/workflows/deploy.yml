name: Build and deploy page

on:
  push:
    branches: ["main"]

permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Install Emacs
        run: |
          sudo apt-get update
          sudo apt-get install -y emacs-nox graphviz

      - name: Build Static Site
        run: emacs --batch -l web-build.el

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: './web-doc'

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4